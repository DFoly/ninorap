# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
warnings_are_errors: false

# builed failed without force install
#install:
#- R -e 'devtools::install_github("ukgovdatascience/govstyle", force=TRUE)'

os:
 - linux
